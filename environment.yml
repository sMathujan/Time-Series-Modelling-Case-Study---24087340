name: electricity-demand-forecasting
channels:
  - conda-forge
dependencies:
  - python=3.11
  - numpy>=1.24
  - pandas>=2.0
  - scipy>=1.10
  - statsmodels>=0.14
  - scikit-learn>=1.3
  - matplotlib>=3.7
  - requests>=2.31
  - pytest>=7.0
  - pip
  - pip:
      - tensorflow>=2.15   # optional, for the LSTM
